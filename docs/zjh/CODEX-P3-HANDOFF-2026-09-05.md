# P3 接手交接 · 2026-09-05


> **用户已要求停止开发并交接 Claude。** 当前修改全部留在 `/Users/mdd/zjh-codex-p3` 的工作树，最新实现**未提交、未推送**；Git HEAD 仍为 `338dd86`。不要只 checkout HEAD 就开始复查，也不要运行 `git reset/clean` 丢掉这些文件。原主工作区和原 P3 工作区均保留。
>
> 已停止本轮完整测试、第二次模型生成和本地 QA 服务；没有继续实施修复、没有部署。接手者应先看工作树 diff 与下列失败项，再决定修改顺序。

## 发布结论

**这是待复查的集成分支，不是已完成的发布。验收仍有失败，未部署。** 不要因类型检查、构建或分类器验证通过而部署。最终本轮测试结果见 [验收记录](acceptance-2026-09.md)。

用户授权：先按文档完成剩余工作、推送 Git、写交接，完成后部署 VPS 96。跨版本的真人 P2 数据分析暂缓；不改动 send-tradfi，也不部署 VPS 107。本文没有将“明天 Claude 复查”理解成必须再次申请部署权限：真正的阻碍是未通过的质量门槛与下述验收歧义。

## 从哪里接手

- 原主工作区 `/Users/mdd/zjh`：`rewrite/self-hosted-realtime`，基准 `f150dd92e5c635eb769c792b143bdb3718765be5`。保留原未跟踪截图及文件。
- 原 P3 工作区 `/Users/mdd/zjh-p3int`：`p3/integrate`，基准 `4258b99`，另有未提交接线修改。未覆盖该目录。
- 本次独立工作区 `/Users/mdd/zjh-codex-p3`：`codex/zjh-p3-completion`。`f2d3c7e` 保存继承的修改；`338dd86` 合入主线梭哈规则，之后才是本次实现。
- 继承补丁 SHA-256：`66e9e7ca778529235d2fec64dd175918d724434375840566780c454e22c93ce5`。原 `tempo.ts` SHA-256：`940e005653ca0eaa206001ed5c52849473aaec55e4967f09b161853c8277ba03`。
- Claude 本机项目记忆及文档、代码核对结果整理于 [项目记忆与状态](PROJECT-MEMORY-AND-STATUS-2026-09-05.md)。这是读取并整理到仓库，未声称回写 Claude App 的内部记忆。

主线 `e958ede` / `f150dd9` 已包含“主动梭哈全押、短码全押跟注、边池结算”的规则和固定牌面的回归；这些是合入的既有修改，不应算成本次新发明。

## 本次改了什么

1. **公开学习与似然表。** 新增 `learned.ts`、离线生成器及模型产物。两种发牌档、八个人物和常人对照，按独立 100 局区块划分训练/留出。统计只含公开动作和用时；暗牌仅作模拟器训练标签。按合法动作机会归一化，下注档位分层，稀疏格平滑并退回粗表/解析先验。闷牌动作不推断暗牌。分类器有最小样本、置信度和分布外未知处理。源码指纹失配直接使测试失败。
2. **三局近因记忆。** 记录每手增量与最近三局窗口，将 R17 接到读人；旧 JSON 没有字段时仍可读。补水合并累加公开计数并按时间保留三个窗口。新增实际结算、JSON 往返和合并回归。
3. **稳定社交身份。** 报复、连续受压、被诈及印象统一使用长期身份，兼容当前房间旧 ID。真正比掉该玩家的对手承担淘汰归因；诈唬被抓只看本手动作，避免终身累计造成误记。
4. **节奏和定向表情。** 继承的用时塑形与读用时已接到决策；补 R25 展示欲、基础看牌节奏、协议和 UI 的目标昵称。自动跟注提示改为“全押跟注”，与主线房规一致。
5. **人物决策接线与校准。** 补齐两系统的比牌把握、全押把握和人物风险偏好；修正没有弃牌概率仍会进入偷池线的问题；调整若干卡的弱牌原型、看牌习惯和节奏。此项对行为影响较大，**尚未达到完整行为验收，必须重点审查**，不能把手调系数当作真人数据校准。
6. **验收修正。** 场景采样不再反复污染同一份情绪状态；改用当前发牌档的手牌分位；区分弱牌价值加注与偷池线；自对弈写入机器人用时；“挑最弱”按其公开范围模型判定。移除“未兑现反而通过”的收益断言，四条收益假设分别报告。老陈偷池对照统一加注时点；老王脚本不再用另一分支故意坏打来抵消对照收益。
7. **文档整理。** 旧人物长报告移到 `archive/personas-draft-2026-09-03.md`；当前目录列清 14 项接线。同步 README 发牌档、八个人物、记忆和 R17/R25 描述。历史报告中的旧数字不能当作当前通过证据。

## Claude 优先复查

### P0：发布前必须解决

- 按验收记录重跑完整测试；逐个解决行为失败。不得下调数值、换种子、缩窄样本或把失败改成 TODO 来获得全绿。
- **§6.5 验收冲突**：要求每张卡能被其利用脚本净胜，又要求同一脚本不净胜阿杰和小林，对两张卡自身存在矛盾。已向用户提出澄清，候选口径为“目标相对无脚本的收益增量，再与其他人格对照”或“两张基准卡只验收信息/行为破绽”。得到明确结论后统一文档、脚本和阈值；其余失败不依赖这一歧义，也必须解决。
- **选线稳定性**：`choosePlan()` 调 `sit.rng`，实际来自包含 `actionSeq` 的 `botRoll()`；`planRoll()` 存在但未被使用。原有“一致性 ≥90%”测试只判每步动作是否符合当步所选线路，不能证明跨动作守住同一线路。要增加跨动作、不发生新信息时的测试，并按 §4.4 修复状态/随机源。仅换固定随机数仍不足以保证权重变化时不换线。
- **数学型下注尺寸**：价格条件似然“读尺寸”已接上，但不等于实现了数学型“按目标底池选尺寸”。`lookahead.ts::continueValue` 仍用行动者成本估对手后续投入，没有完整体现对手看牌倍率与剩余筹码；前瞻未来成本也需检查。需独立算术用例和行为证据，禁止仅将线性相关门槛改小。
- **评估信息边界**：检查所有模拟器均给 `applyCommand` 传真实模拟用时；脚本没有用时时保持未知，不能伪造。非法动作应使评估失败，不能悄悄换成跟/弃再将收益归给原脚本。B 线已移除这类回退，A 线和旧竞技场兼容路径仍需逐项审查。
- 多数人物源文件的长注释/`leaks` 还保留前期测量和“尚无消费方”等旧描述，不能当现状依据。行为冻结后彻底整理，并重新生成源码绑定模型。

### P1：完成发布证据

- 冻结源码后生成两次模型并比较 SHA-256；重新跑完整测试、类型检查、构建及浏览器/WS/CLI/MCP 冒烟。
- 分类器当前验证是模拟器分布内区块，不能宣传为真人识别准确率。当前特征没有完整覆盖设计列出的 VPIP、遇压弃牌与挑人偏好，要核对验收究竟要求这些具体特征还是允许其他公开统计。
- 检查持续累计与 100 局训练区块的分布差异；验证真人迟延/断线不会被强行判为某人格。分布外阈值是保护措施，不是校准证据。
- 在 1280×720 活跃牌局检查顶座手牌区域与顶栏是否重叠；本轮看到了定向表情的目标文本，但没有完成全尺寸视觉验收。
- 完成后重新只读审计 VPS，再实施带校验和、在线 SQLite 备份及回滚路径的部署；不能直接套首次安装脚本覆盖已有服务。
- §6.6 的 30 局真人验收必须由真人执行。本轮本地自动牌局不计入真人验收。

## 真人 P2 数据：暂缓，不混用

只读取样含 4,941 条机器人决策、1,390 条真人动作、379 条结果；剔除冒烟后候选为 4,275 / 1,265 / 331，横跨多个版本，真人样本集中于少量玩家。用户已明确先不做这组基础模型优化分析。私人原始数据留在本机受限目录，不入 Git，不上传 VPS。

快照 SHA-256：`e667a609ad12a66acae072391ca21183540d6228221b090aa129a7dee8521be2`。本轮没有以这份混版数据训练或证明新的模型优越。

## VPS 与部署方式

- 仅 `trade-vps-old` / VPS 96：服务 `zjh`，目录 `/opt/zjh/dist`，数据库 `/var/lib/zjh/zjh.db`，监听 `127.0.0.1:8787`。
- 正确健康地址 `/healthz`；`/health` 会返回 SPA，不能作为探活。
- 本轮只读审计仍为 `deployed f150dd9 at 20260904T191610Z`，Node `v22.23.2`，服务运行、重启计数 0、内存约 51 MiB / 512 MiB、磁盘 58%，房间/玩家/连接均 0。时间点和后续结果以验收记录为准。
- 待发布流程：重新检查在线玩家与数据库完整性 → 生成在线 SQLite 备份并验证 → 上传版本目录并逐文件核对 SHA-256 → 保存旧目录和版本 → 原子切换 → 重启 → `/healthz` 与 WS 验证 → 检查日志、重启数、数据库可读性。失败恢复旧目录及版本；不要自动恢复数据库，除非有独立恢复理由。

## 复现

```sh
cd /Users/mdd/zjh-codex-p3
npm run typecheck
node scripts/zjh-generate-model.ts 2000
npm test
npm run build
```

本轮最终验证使用与 VPS 相同版本的 Node `22.23.2`（macOS arm64）；普通终端默认 Node 26，复查时需明确版本。Linux VPS 只做了只读审计，不能把 macOS 测试称为 Linux 部署验收。


## 停止时的准确现场

- 完整 Node 22 回归 `full-17.log` 被用户停止指令中断，没有最终汇总，**不能记为通过**。上一轮完整结果仍是 518 项 / 500 通过 / 18 失败；后续评测修正后失败数可能改变。
- 首次最终模型生成 `model-17.log` 完成：36,000 局、64/64 留出区块正确。源码指纹 `5fe44a6be78074066032b0af11b088e247fe3b705b82e0c58ff7396f57b0c5df`；模型文件 SHA-256 `1caf67b752c33fb8ab14c6bc97ef0b5422ba6030cc89db081983c942d6619347`；验证报告 SHA-256 `cc43424f8acc3f3cc661088aa71cad66f55741728534d0852b143d044332b69d`。
- 独立目录的第二次模型生成被中断，**没有完成双跑确定性验证**。可删除并重建该可复现目录，不要误删主工作树模型。
- Node 22 类型检查、Vite 客户端构建、server/CLI/MCP 打包均已完成；最终产物的 CLI/MCP/WS 冒烟尚未进行。旧 QA 牌局已结束，进程已停。
- 本机日志与继承补丁：`/Users/mdd/TradeFiData/zjh-p3-handoff-20260905/`。重点日志：`full-14.log`（完整失败证据）、`full-17.log`（中断）、`model-17.log`（完成）、`model-repeat-17.log`（中断）、`typecheck-node22-17.log`、`build-client-17.log`、`build-server-17.log`。目录里还有私人测试 DB，不要整目录上传 Git。
- 本次过程中试验过换手号采样及继续调整三张卡，随后已撤回这些收尾试验。实际交接内容以 `git diff` 为准；对采样随机源曾给出错误判断，复核后已更正为上面的“选线稳定性”问题。

Claude 的第一步建议：读取本文与验收记录，执行 `git status --short`、`git diff --stat`，先独立确认已实现的 14 项接线与评测缺口；不要把前任写的效果解释直接当结论。解决质量门槛后再提交、推送和部署，用户最新的停止指令不授权本线程继续这些动作。
